import React from 'react';
import CycleRoute from "./cycle-routes";

export default class Layout extends React.Component {
    render() {
        return (
            <CycleRoute/>
        );
    }
}
